<div class="blog-masthead">
  <div class="container">
    <nav class="blog-nav">
      <a class="blog-nav-item" href="{{../config.baseURL}}/"> Home </a>
      {{#each collections as |value key|}}
        <a class="blog-nav-item" href="{{../config.baseURL}}/{{value.[0].path}}">{{value.[0].title}} </a>
      {{/each}}
    </nav>
  </div>
</div>
